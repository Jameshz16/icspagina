export const metadata = {
  title: "Our Projects | Portfolio of Restoration & Pools",
  description: "View our gallery of completed concrete restoration projects, custom pools, commercial painting, and new construction work.",
};

export default function ProjectsLayout({ children }) {
  return <>{children}</>;
}
