export const metadata = {
  title: "Custom Pool Construction & Diamond Brite Finishes | ICS",
  description: "Luxury pool design, new construction, resurfacing, and Diamond Brite plaster finishes. Transform your backyard with ICS.",
};

export default function PoolsLayout({ children }) {
  return <>{children}</>;
}
